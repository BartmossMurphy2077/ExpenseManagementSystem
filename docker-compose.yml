version: "3.9"

services:
  backend:
    build: ./backend
    container_name: expense_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data  # mount host folder to persist SQLite DB
